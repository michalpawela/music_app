import 'package:flutter/material.dart';

class PlaylistCreatePage extends StatelessWidget {
  const PlaylistCreatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
